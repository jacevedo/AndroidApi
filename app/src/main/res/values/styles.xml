<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Here we setting appcompat’s actionBarStyle -->
       <!-- <item name="actionBarStyle">@style/ActionBarTheme</item>-->

        <!-- ...and here we setting appcompat’s color theming attrs -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>

        <item name="colorAccent">@color/accent</item>
        <item name="android:textColorPrimary">@color/primary_text</item>
        <item name="android:textColorSecondary">@color/secondary_text</item>
        <item name="android:icon">@color/icons</item>
        <item name="android:divider">@color/divider</item>
        <item name="android:windowBackground">@color/windowBackground</item>



        <!-- You can also set colorControlNormal, colorControlActivated
             colorControlHighlight & colorSwitchThumbNormal. -->

        <!-- The rest of your attributes -->
    </style>
    <style name="ActionBarTheme">

    </style>

    <style name="DialogAnimation">
        <item name="android:windowEnterAnimation">@anim/entrada_arriba</item>
        <item name="android:windowExitAnimation">@anim/salida_abajo</item>
    </style>
    <style name="DialogAnimationZoom">
        <item name="android:windowEnterAnimation">@anim/entrada_zoom</item>
        <item name="android:windowExitAnimation">@anim/salida_zoom</item>
    </style>
</resources>
